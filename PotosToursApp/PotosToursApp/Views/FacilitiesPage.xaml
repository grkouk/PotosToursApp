<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
                xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
              prism:ViewModelLocator.AutowireViewModel="True"
                x:Class="PotosToursApp.Views.FacilitiesPage">
   
    <ContentPage.Content>
        <!--<StackLayout>
            <Entry Placeholder="Facility Name" Text="{Binding Name}" />
            <Entry Placeholder="Short Description" Text="{Binding ShortDescription}" />
            <Entry Placeholder="Description" Text="{Binding Description}" />
            <Button Text="Save" Command="{Binding SaveCommand}"></Button>
        </StackLayout>-->
        <ListView ItemsSource="{Binding FacilitiesCollection}">  
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Name}" Detail="{Binding ShortDescription}"></TextCell>
                    
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>